// Production environment — replace these values with real deployed service URLs before going live
export const environment = {
  production: true,
  apiUrl: 'https://your-api-domain.com/api/v1',
  restaurantsApiUrl: 'https://your-api-domain.com/restaurants/api',
  ordersApiUrl: 'https://your-api-domain.com/orders/api'
};
